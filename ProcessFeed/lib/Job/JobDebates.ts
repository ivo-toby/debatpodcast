import { DebatDay } from '../debatDirect/debatTypes';
import { AbstractJob } from './AbstractJob';
import { AbstractJobType } from './types';

export class JobDebates extends AbstractJob<DebatDay> implements AbstractJobType {

}
